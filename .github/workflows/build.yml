name: Build

on: [push]

env:
  VERSION: ${{ github.run_number }}

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v1
      - name: publish
        run: make publish